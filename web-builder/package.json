{
  "name": "mahiro-auth-web-builder",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "dev": "bun run.ts",
    "build": "bun build.ts",
    "release": "bun run build && bun release.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.25.2",
    "octokit": "^4.1.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/adm-zip": "^0.5.7",
    "adm-zip": "^0.5.16",
    "ora": "^8.2.0",
    "yaml": "^2.7.1"
  }
}