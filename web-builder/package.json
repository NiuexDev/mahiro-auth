{
  "name": "mahiro-auth-web-builder",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "bun build.ts",
    "release": "bun run build && bun release.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.25.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}