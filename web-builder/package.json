{
  "name": "mahiro-auth-web-builder",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "bun build.ts",
    "release": "bun run build && bun release.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.25.2",
    "octokit": "^4.1.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}